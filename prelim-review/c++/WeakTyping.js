let x = "5";  // string
let y = 2;    // number

// JS will automatically decide what to do

console.log(x + y);  // "52"  (string concatenation)
console.log(x * y);  // 10   (string converted to number)